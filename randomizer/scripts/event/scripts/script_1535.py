# pylint: disable=C0301

"""E1535_CLONE_RESERVED"""

from randomizer.scripts.event.script_imports import *

script = EventScript(
    [
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
        EndAll(),
    ]
)
